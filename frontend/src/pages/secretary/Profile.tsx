import React from 'react';
import { Camera, CheckCircle2, Mail, MapPin, ShieldCheck, UserRound, Users } from 'lucide-react';
import { Card, CardContent, CardHeader, CardTitle } from '../../components/Card';
import { getCurrentSecretary } from './utils';

export const Profile: React.FC = () => {
  const secretary = getCurrentSecretary(); const name = secretary?.name || 'Class Secretary'; const initials = name.split(' ').filter(Boolean).slice(0, 2).map(part => part[0]).join('').toUpperCase() || 'S';
  const details = [
    { label: 'Email address', value: secretary?.email || 'Not available', icon: Mail },
    { label: 'Assigned class', value: secretary?.assignedClassName || 'Clinical Rotation A', icon: Users },
    { label: 'Classroom', value: secretary?.classroomName || 'Dental Clinic B - Room 402', icon: MapPin },
    { label: 'CCTV assignment', value: secretary?.cctvCameraId || 'CCTV-CLINIC-A-01', icon: Camera },
  ];
  return <div className="space-y-6 animate-fade-in max-w-7xl mx-auto"><div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 border-b border-slate-200 dark:border-slate-800 pb-4"><div><h1 className="text-2xl font-extrabold font-heading text-slate-800 dark:text-slate-100">Class Secretary Profile</h1><p className="text-xs text-slate-400 mt-1">Review the account and classroom scope assigned to your attendance role.</p></div><div className="inline-flex items-center gap-2 text-[10px] font-bold uppercase tracking-wider text-blue-700 dark:text-blue-400 bg-blue-50 dark:bg-blue-950/30 px-3 py-2 rounded-xl"><CheckCircle2 className="w-3.5 h-3.5" />Assigned access active</div></div><div className="grid grid-cols-1 lg:grid-cols-12 gap-5"><div className="lg:col-span-4 space-y-5"><Card className="p-0 overflow-hidden"><div className="h-20 bg-gradient-to-r from-blue-600 to-sky-500" /><CardContent className="relative pt-0 pb-5"><div className="-mt-10 w-20 h-20 rounded-2xl bg-white dark:bg-slate-900 p-1 shadow-lg"><div className="w-full h-full rounded-xl bg-gradient-to-tr from-blue-200 to-sky-200 dark:from-blue-800 dark:to-sky-900 flex items-center justify-center text-xl font-extrabold text-blue-700 dark:text-blue-300">{initials}</div></div><h2 className="mt-3 text-base font-bold text-slate-800 dark:text-slate-100">{name}</h2><p className="text-xs text-blue-600 dark:text-blue-400 font-semibold mt-0.5">{secretary?.title || 'Class Secretary'}</p></CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2 text-sm"><ShieldCheck className="w-4.5 h-4.5 text-blue-500" />Access scope</CardTitle></CardHeader><CardContent className="text-xs text-slate-500 dark:text-slate-400 leading-relaxed">You can review class attendance, submit manual overrides, and view your assigned classroom camera. Account and assignment changes require an administrator.</CardContent></Card></div><Card className="lg:col-span-8 p-0 overflow-hidden"><CardHeader className="border-b border-slate-100 dark:border-slate-800/80"><CardTitle className="flex items-center gap-2 text-sm"><UserRound className="w-4.5 h-4.5 text-blue-500" />Assignment details</CardTitle></CardHeader><CardContent className="p-5"><div className="grid sm:grid-cols-2 gap-3">{details.map(({ label, value, icon: Icon }) => <div key={label} className="p-4 rounded-xl bg-slate-50/80 dark:bg-slate-900/60 border border-slate-150 dark:border-slate-800"><div className="flex items-center gap-2 text-slate-400"><Icon className="w-4 h-4 text-blue-500" /><span className="text-[10px] font-bold uppercase tracking-wider">{label}</span></div><p className="mt-2 text-sm font-bold text-slate-800 dark:text-slate-100">{value}</p></div>)}</div><div className="mt-5 p-3.5 rounded-xl bg-blue-50/70 dark:bg-blue-950/20 border border-blue-100 dark:border-blue-900/30 text-xs text-blue-800 dark:text-blue-300">This information is read-only to protect classroom and CCTV access assignments.</div></CardContent></Card></div></div>;
};
