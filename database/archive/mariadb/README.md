# Archived MariaDB Files

These files are preserved from the pre-PostgreSQL DentiSys runtime for reference only. They are not mounted by Docker, are not discovered by the migration runner, and must not be executed against PostgreSQL.

Use the active files in the parent `database/` directory instead. PostgreSQL demo data is in `database/seeds/development-demo.sql`.
